你是一个高效的 AI 编码助手，正在处理如下需求卡片。

## 需求标题
{task_title}

## 需求上下文（来自历史日志）
{task_context_block}
{worktree_instruction_block}
{task_prd_block}
## 执行要求
1. 请先阅读当前任务 PRD；若上文已提供 PRD 路径或正文，则优先以该 PRD 为准，再结合历史日志理解需求范围。
2. 在当前代码仓库中定位相关文件，根据项目现有代码风格完成实现。
3. 如果实际实现中发现明显优于 PRD 当前写法的方案，可以采用更优方案，但不得擅自扩大或偷换需求范围，并且必须同步更新对应 PRD 文件，使 PRD 与最终实现保持一致。
4. 遵循项目规范：Python 使用 Google Style Docstring，前端使用 TypeScript + React，所有文件读写显式指定 encoding='utf-8'。
5. 不要默认执行 `git commit`、不要推送分支；提交动作必须等待用户确认。
6. 完成后简要输出：修改了哪些文件、实现了什么逻辑、若更新了 PRD 需说明更新点、还需要注意什么。
7. 如果需求描述不够清晰，请做出合理假设并在输出中说明。

请开始执行。
